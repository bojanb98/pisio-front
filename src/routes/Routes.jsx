import { BrowserRouter as Router, Switch } from "react-router-dom";


const Routes = (_) => {
   return (
      <Router>
         <Switch>

         </Switch>
      </Router>
   );
};

export default Routes;
